# Forge Examples

Two slightly more complete demo apps live here:

- [mission-control](mission-control/README.md): a Python dashboard for release status and rollout events.
- [signal-hub](signal-hub/README.md): a Go operations console that shows the same Forge deployment story in another language.

Both apps are self-contained and include their own `forge.yaml` manifests.