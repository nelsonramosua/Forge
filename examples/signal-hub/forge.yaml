name: signal-hub
runtime: go1.21
build:
  commands:
    - go build -o signal-hub .
run:
  command: ./signal-hub
  port: 8000
resources:
  memory: 128M
  cpu: 0.3
health:
  path: /healthz
  interval: 3s
  timeout: 2s
  retries: 10
env:
  - HUB_TITLE
  - HUB_REGION
  - GIT_COMMIT